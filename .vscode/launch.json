{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "edge",
      "request": "launch",
      "name": "localhost (Edge)",
      "applicationUrl": "https://localhost:7185",
      "webRoot": "${workspaceFolder}"
    },
    {
      "type": "chrome",
      "request": "launch",
      "name": "localhost (Chrome)",
      "applicationUrl": "https://localhost:7185",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
